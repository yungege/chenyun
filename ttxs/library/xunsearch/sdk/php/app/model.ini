project.name = model
project.default_charset = utf8
server.index = 8383
server.search = 8384

[view_id]
type = id

[user_id]

[name]
type = title
tokenizer = scws(8)

[py_name]
index = both
tokenizer = xstep(1)

[pre_py_name]
index = both
tokenizer = xstep(1)

[is_original]
index = self
tokenizer = full

[collection_count]
type = numeric

[like_count]
type = numeric

[download_count]
type = numeric

[comment_count]
type = numeric

[view_count]
type = numeric

[print_count]
type = numeric

[has_print_params]
index = self
tokenizer = full

[category_first]
index = self
tokenizer = full

[category_second]
index = self
tokenizer = full

[category_third]
index = self
tokenizer = full

[origin]
index = self
tokenizer = full

[is_open]
index = self
tokenizer = full

[upload_stamp]
type = numeric

