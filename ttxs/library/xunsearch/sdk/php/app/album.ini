project.name = album
project.default_charset = utf8
server.index = 8383
server.search = 8384

[album_id]
type = id

[user_id]

[name]
type = title
tokenizer = scws(8)

[py_name]
index = both
tokenizer = xstep(1)

[pre_py_name]
index = both
tokenizer = xstep(1)

[focus_count]
type = numeric

[view_count]
type = numeric

[is_open]
type = numeric

[add_stamp]
type = numeric

[model_view_count]
type = numeric
