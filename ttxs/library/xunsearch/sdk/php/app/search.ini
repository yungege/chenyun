project.name = search
project.default_charset = utf8
server.index = 8383
server.search = 8384

[id]
type = id

[file_id]

[name]
type = title
tokenizer = scws(8)

[py_name]
index = both
tokenizer = xstep(1)

[pre_py_name]
index = both
tokenizer = xstep(1)

[type]
index = self
tokenizer = full
