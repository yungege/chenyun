project.name = seekmodel
project.default_charset = utf8
server.index = 8383
server.search = 8384

[sm_id]
type = id

[title]
type = title
tokenizer = scws(8)

[py_title]
index = both
tokenizer = xstep(1)

[pre_py_title]
index = both
tokenizer = xstep(1)

[reward_egg]
type = numeric

[has_answer]
index = self
tokenizer = full

[comment_count]
type = numeric

[adopt_stamp]
type = numeric

[add_stamp]
type = numeric
