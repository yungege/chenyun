project.name = user
project.default_charset = utf-8
server.index = 8383
server.search = 8384

[user_id]
type = id

[nickname]
type = title
tokenizer = scws(8)

[py_name]
index = both
tokenizer = xstep(1)

[pre_py_name]
index = both
tokenizer = xstep(1)

[album_count]
type = numeric

[print_result_count]
type = numeric

[model_view_count]
type = numeric

[be_cared_count]
type = numeric

[register_stamp]
type = numeric

[is_designer]
index = self
tokenizer = full

[is_old_user]
index = self
tokenizer = full
