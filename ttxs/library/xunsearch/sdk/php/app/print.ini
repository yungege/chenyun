project.name = print
project.default_charset = utf8
server.index = 8383
server.search = 8384

[print_id]
type = id

[view_id]
index = both

[view_name]
type = title
tokenizer = scws(8)

[py_name]
index = both
tokenizer = xstep(1)

[pre_py_name]
index = both
tokenizer = xstep(1)

[view_count]
type = numeric

[stamp]
type = numeric
